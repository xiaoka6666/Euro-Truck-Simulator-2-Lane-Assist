<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:ETS2LA.UI.Views"
             xmlns:husk="https://github.com/d3ara1n/Huskui.Avalonia"
             x:Class="ETS2LA.UI.Views.ManagerView"
             x:DataType="views:ManagerView">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="WrapPanelTemplate">
            <WrapPanel />
        </ItemsPanelTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <StackPanel>
            <ItemsControl x:Name="PluginList" 
                          ItemsSource="{Binding Plugins}" 
                          ItemsPanel="{StaticResource WrapPanelTemplate}"
                          IsVisible="{Binding HasPlugins}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="views:PluginItem">
                        <husk:Card 
                            Padding="14"
                            Margin="10"
                            Width="332"
                            HorizontalAlignment="Left"
                            Tag="{Binding}"
                            Cursor="Hand"
                            Classes.Highlight="{Binding IsEnabled}"
                            PointerPressed="OnCardPointerPressed"
                        >
                            <StackPanel Spacing="6" Width="302">
                                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <husk:Tag IsVisible="{Binding IsEnabled}" Classes="Primary">
                                        <TextBlock Text="已启用" FontSize="12" FontWeight="450" />
                                    </husk:Tag>
                                    <TextBlock Text="{Binding Name}" FontSize="14" />
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" Classes="Description" FontSize="12" />
                            </StackPanel>
                        </husk:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border x:Name="PlaceholderPanel" IsVisible="{Binding !HasPlugins}">
                <StackPanel Spacing="6">
                    <TextBlock Text="未找到插件" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="将插件 DLL 拖放到 Plugins 文件夹中（或构建现有插件项目），然后点击刷新。" />
                    <Button Content="刷新" Width="100" Click="OnRefreshButtonClick"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
